<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <style>
    body { margin: 0; padding: 0; font-family: Arial, sans-serif; background: #2f3e46; color: #fff; }
    .card { width: 380px; margin: 80px auto; padding: 24px; background: linear-gradient(135deg, #667eea, #764ba4); border-radius: 12px; box-shadow: 0 10px 25px rgba(0,0,0,0.2); }
    form { display: flex; flex-direction: column; gap: 12px; }
    input, button { padding: 10px; border-radius: 8px; border: none; }
    button { background: #1d4ed8; color: white; cursor: pointer; }
    .alert { background: #fee2e2; color: #991b1b; padding: 10px; border-radius: 8px; margin-bottom: 12px; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Login</h1>
    <% if (message) { %>
      <div class="alert"><%= message %></div>
    <% } %>
    <form action="/api/auth/login" method="post">
      <label for="email">Email</label>
      <input type="email" name="email" placeholder="Enter your email" required>
      <label for="password">Password</label>
      <input type="password" name="password" placeholder="Enter your password" required>
      <button type="submit">Login</button>
    </form>
    <p>Don't have an account? <a href="/register" style="color: #fff;">Register</a></p>
  </div>
</body>
</html>